jsondata

# Environment files — never commit real company IDs, connection strings or secrets.
# Applies repo-wide (backend, frontends). Templates (.env.example) stay tracked.
.env
.env.*
**/.env
**/.env.*
!.env.example
!**/.env.example
